{
  "name": "webgl-school-001",
  "version": "1.0.0",
  "description": "npm i",
  "main": "index.js",
  "scripts": {
    "start": "npm run dev",
    "dev": "browser-sync ./docs/ -w",
    "stage": "rsync -av --delete --exclude .DS_Store --exclude .git --exclude node_modules --exclude package.json --exclude package-lock.json --exclude .editorconfig --exclude .gitignore --exclude dist ./dist/ ",
    "dist": "rsync -av --delete --exclude .DS_Store --exclude .git --exclude node_modules --exclude package.json --exclude package-lock.json --exclude .editorconfig --exclude .gitignore --exclude dist ./docs/ dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^3.0.2"
  }
}
